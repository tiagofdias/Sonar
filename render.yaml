services:
  # Full-stack application (backend + frontend)
  - type: web
    name: sonar-app
    env: node
    plan: free
    buildCommand: |
      # Build both frontend and backend
      npm run build
      # Copy frontend build into backend for serving
      cp -R frontend/dist backend/dist
      # Install backend dependencies
      npm install --prefix backend
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        value: mongodb+srv://tiagodiascl:cbYZeh2iovHFwiwp@cluster0.at1qu.mongodb.net/realtalk_db?retryWrites=true&w=majority&appName=Cluster0
      - key: JWT_SECRET
        value: mysecretkey
      - key: CLOUDINARY_CLOUD_NAME
        value: dv38vvlpg
      - key: CLOUDINARY_API_KEY
        value: 673249356813619
      - key: CLOUDINARY_API_SECRET
        value: O3aNgVA0cA7Twe47KA4F1ssH7JU
