{
  "name": "sonar-chat-app",
  "version": "1.0.0",
  "description": "A real-time chat application",
  "main": "backend/src/index.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && xcopy /E /I /Y frontend\\dist backend\\dist",
    "start": "npm run start --prefix backend",
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "build:linux": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && cp -r frontend/dist backend/dist"
  },
  "keywords": ["chat", "realtime", "socket.io", "react", "express"],
  "author": "",
  "license": "ISC"
}
